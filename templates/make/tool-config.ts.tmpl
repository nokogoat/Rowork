import type { ToolDefinition } from "./ToolDefinition";

export const {{ constName }}: ToolDefinition = {
	name: "{{ base }}",
	tag: "{{ tag }}",
	cooldown: {{ cooldown }},
	canBeDropped: {{ canBeDropped }},
	giveOnSpawn: {{ giveOnSpawn }},
};
