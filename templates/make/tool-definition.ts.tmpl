/**
 * Describes one tool. Each `rowork make:tool` adds a constant of this type in
 * this directory: it is the single place where the tool's settings live.
 */
export interface ToolDefinition {
	/** Name shown in the player's backpack. */
	readonly name: string;
	/** CollectionService tag that makes Flamework attach the tool's component. */
	readonly tag: string;
	/** Minimum seconds between two uses. */
	readonly cooldown: number;
	/** Can the player drop it on the ground? */
	readonly canBeDropped: boolean;
	/** Given to every player each time they spawn. */
	readonly giveOnSpawn: boolean;
}
